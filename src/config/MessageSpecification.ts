import { MessageSpecificationInterface, Message } from '../';

export class MessageSpecification implements MessageSpecificationInterface {

	isSatisfiedBy(message: Message): boolean {
		// TODO: Implement
		return false
	}
}